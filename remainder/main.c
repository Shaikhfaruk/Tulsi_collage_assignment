#include <stdio.h>

int main()
{
    int dividend, divisor, quoient, remainder;

    printf("Enter Dividend: ");
    scanf("%d", &dividend);

    printf("Enter divisor");

    quoient = dividend / divisor;

    printf("Quotient = %d\n", quoient);
    printf("Remainder = %d\n", remainder);
    return 0;
}